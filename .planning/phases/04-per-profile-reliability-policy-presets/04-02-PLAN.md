---
phase: 04-per-profile-reliability-policy-presets
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs
autonomous: true
must_haves:
  truths:
    - "A user-selected reliability preset can be applied from deterministic ViewModel command flow."
    - "Applying a preset updates only managed reliability keys and leaves unrelated mount options untouched."
    - "Selected preset id persists in profile storage and reloads when profiles are reopened."
  artifacts:
    - path: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      provides: "Preset selection state, apply command, and persistence mapping"
      contains: "ApplyReliabilityPreset"
    - path: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      provides: "Persisted profile contract includes selected preset id"
      contains: "PersistedProfile"
  key_links:
    - from: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      to: "RcloneMountManager.Core/Models/ReliabilityPolicyPreset.cs"
      via: "Catalog lookup and deterministic apply path"
      pattern: "GetById|Catalog|ApplyReliabilityPreset"
    - from: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      to: "profiles.json"
      via: "LoadProfiles/SaveProfiles mapping for selected preset id"
      pattern: "JsonSerializer|SelectedReliabilityPresetId"
---

<objective>
Implement deterministic preset selection/apply/persist logic in the ViewModel so reliability behavior is driven by policy presets instead of raw flag edits.

Purpose: Deliver POL-02 command path and persistence wiring while preserving existing MVVM runtime-state architecture.
Output: `MainWindowViewModel` supports preset selection, managed-key patch apply, and save/load mapping for selected preset id.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-per-profile-reliability-policy-presets/04-RESEARCH.md
@.planning/phases/04-per-profile-reliability-policy-presets/04-01-SUMMARY.md
@RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs
@RcloneMountManager.Core/Models/ReliabilityPolicyPreset.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ViewModel preset state and deterministic apply command</name>
  <files>RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs</files>
  <action>Add observable ViewModel state for available reliability presets and selected preset id binding target, then implement `[RelayCommand] ApplyReliabilityPreset` to patch only managed reliability keys in `SelectedProfile.MountOptions`. Remove existing values for managed keys first, write preset overrides second, and keep all non-managed option entries unchanged. Ensure command updates both profile state and `MountOptionsVm` view-model projection so UI and persisted data share one source of truth. Keep explicit guard behavior for non-rclone mount types (do not apply rclone reliability keys to NFS profiles).</action>
  <verify>dotnet test RcloneMountManager.slnx --filter "FullyQualifiedName~MainWindowViewModel"</verify>
  <done>Preset apply path is deterministic, profile-scoped, and non-clobbering for unrelated options.</done>
</task>

<task type="auto">
  <name>Task 2: Persist and reload selected preset id in profile mapping</name>
  <files>RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs</files>
  <action>Update `LoadProfiles`, `SaveProfiles`, and nested `PersistedProfile` mapping to include `SelectedReliabilityPresetId` with migration-safe fallback to `balanced` when missing. Ensure profile-change dirty tracking includes this property so manual preset changes are saved by existing save flow. Keep serialization style consistent with existing `profiles.json` payload shape and avoid introducing separate policy persistence files.</action>
  <verify>dotnet build RcloneMountManager.slnx</verify>
  <done>Selected preset id survives save/reload across app sessions through existing profile persistence pipeline.</done>
</task>

</tasks>

<verification>
Run `dotnet test RcloneMountManager.slnx --filter "FullyQualifiedName~MainWindowViewModel"` and `dotnet build RcloneMountManager.slnx` to validate apply behavior and persistence wiring compile correctly.
</verification>

<success_criteria>
ViewModel now supports selecting and applying reliability presets without raw flag editing and persists the selected preset per profile.
</success_criteria>

<output>
After completion, create `.planning/phases/04-per-profile-reliability-policy-presets/04-02-SUMMARY.md`
</output>
