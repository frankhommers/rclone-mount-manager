---
phase: 04-per-profile-reliability-policy-presets
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - RcloneMountManager.GUI/Views/MainWindow.axaml
  - RcloneMountManager.Tests/ViewModels/MainWindowViewModelPolicyPresetTests.cs
autonomous: true
must_haves:
  truths:
    - "User can choose a reliability preset per profile directly in the profile settings UI."
    - "User can apply the selected preset without editing raw rclone flags."
    - "Reopening profile settings shows the previously selected preset and effective reliability options intact."
  artifacts:
    - path: "RcloneMountManager.GUI/Views/MainWindow.axaml"
      provides: "Profile-scoped reliability preset picker and apply action"
      contains: "Reliability"
    - path: "RcloneMountManager.Tests/ViewModels/MainWindowViewModelPolicyPresetTests.cs"
      provides: "Regression coverage for apply/persist/reload/non-clobber behavior"
      contains: "ApplyReliabilityPreset"
  key_links:
    - from: "RcloneMountManager.GUI/Views/MainWindow.axaml"
      to: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      via: "ComboBox selected value + apply command bindings"
      pattern: "SelectedReliabilityPresetId|ApplyReliabilityPresetCommand"
    - from: "RcloneMountManager.Tests/ViewModels/MainWindowViewModelPolicyPresetTests.cs"
      to: "profiles.json persistence output"
      via: "Save/reload assertions for selected preset id and managed options"
      pattern: "SelectedReliabilityPresetId|MountOptions"
---

<objective>
Deliver user-facing preset controls and regression tests proving per-profile reliability preset selection, application, and revisit persistence.

Purpose: Complete POL-01/POL-02 user experience and lock behavior with deterministic tests.
Output: Main window preset controls plus policy preset test suite covering apply, persist/reload, and non-clobber guarantees.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-per-profile-reliability-policy-presets/04-RESEARCH.md
@.planning/phases/04-per-profile-reliability-policy-presets/04-02-SUMMARY.md
@RcloneMountManager.GUI/Views/MainWindow.axaml
@RcloneMountManager.Tests/ViewModels/MainWindowViewModelStartupTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add profile reliability preset picker and apply controls to main window</name>
  <files>RcloneMountManager.GUI/Views/MainWindow.axaml</files>
  <action>In the Profile Settings section, add a reliability policy panel with a `ComboBox` bound to available presets and selected preset id, plus an explicit Apply button bound to `ApplyReliabilityPresetCommand`. Surface concise helper text that this flow manages reliability flags safely without manual raw flag editing. Keep existing layout density/style conventions and ensure controls are scoped to selected profile context.</action>
  <verify>dotnet build RcloneMountManager.slnx</verify>
  <done>User can choose and apply a profile reliability preset directly from UI controls.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression tests for apply, persistence, reload, and non-clobber guarantees</name>
  <files>RcloneMountManager.Tests/ViewModels/MainWindowViewModelPolicyPresetTests.cs</files>
  <action>Create a dedicated ViewModel test file covering at minimum: preset apply writes managed reliability keys; preset apply preserves unrelated mount option keys; selected preset id persists to `profiles.json`; loading from disk restores selected preset id and effective managed options for revisit behavior. Use temp profiles path pattern already used in existing ViewModel tests and keep tests deterministic without UI harnesses.</action>
  <verify>dotnet test RcloneMountManager.slnx --filter "FullyQualifiedName~MainWindowViewModelPolicyPresetTests"</verify>
  <done>Automated tests prevent regressions in policy apply/persist/reload behavior required by POL-01/POL-02.</done>
</task>

<task type="auto">
  <name>Task 3: Run targeted and full build verification for final phase integration</name>
  <files>RcloneMountManager.GUI/Views/MainWindow.axaml, RcloneMountManager.Tests/ViewModels/MainWindowViewModelPolicyPresetTests.cs</files>
  <action>Run targeted policy preset tests, then run solution build to validate integration across Core/GUI/Tests projects. If assertions fail, adjust bindings or ViewModel interactions until tests and build pass without weakening non-clobber guarantees.</action>
  <verify>dotnet test RcloneMountManager.slnx --filter "FullyQualifiedName~MainWindowViewModelPolicyPresetTests" && dotnet build RcloneMountManager.slnx</verify>
  <done>Policy preset UI and ViewModel behavior are validated and ready for execution-phase verification.</done>
</task>

</tasks>

<verification>
Run `dotnet test RcloneMountManager.slnx --filter "FullyQualifiedName~MainWindowViewModelPolicyPresetTests"` then `dotnet build RcloneMountManager.slnx`.
</verification>

<success_criteria>
Phase 4 user flow is complete: users pick a per-profile reliability preset, apply it without raw flag editing, and see the selected preset persist when revisiting settings.
</success_criteria>

<output>
After completion, create `.planning/phases/04-per-profile-reliability-policy-presets/04-03-SUMMARY.md`
</output>
