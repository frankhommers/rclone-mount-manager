---
phase: 04-per-profile-reliability-policy-presets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - RcloneMountManager.Core/Models/ReliabilityPolicyPreset.cs
  - RcloneMountManager.Core/Models/MountProfile.cs
autonomous: true
must_haves:
  truths:
    - "Reliability presets exist as a typed, stable catalog rather than ad-hoc flag strings."
    - "Each profile has an explicit selected reliability preset id that can be persisted and restored."
    - "Preset application scope is constrained to managed reliability keys so unrelated options remain intact."
  artifacts:
    - path: "RcloneMountManager.Core/Models/ReliabilityPolicyPreset.cs"
      provides: "Immutable preset catalog with stable IDs and managed key definitions"
      contains: "ManagedReliabilityKeys"
    - path: "RcloneMountManager.Core/Models/MountProfile.cs"
      provides: "Per-profile selected preset identifier with safe default"
      contains: "SelectedReliabilityPresetId"
  key_links:
    - from: "RcloneMountManager.Core/Models/ReliabilityPolicyPreset.cs"
      to: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      via: "ViewModel consumes typed preset catalog and IDs"
      pattern: "ReliabilityPolicyPreset"
    - from: "RcloneMountManager.Core/Models/MountProfile.cs"
      to: "profiles.json persistence mapping"
      via: "Selected preset id serialized and deserialized"
      pattern: "SelectedReliabilityPresetId"
---

<objective>
Create the typed reliability policy preset foundation so Phase 4 can apply profile-scoped reliability behavior without raw flag editing.

Purpose: Establish a stable preset catalog and per-profile preset identity before ViewModel/UI wiring.
Output: New reliability preset model/catalog and updated `MountProfile` with selected preset id state.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-per-profile-reliability-policy-presets/04-RESEARCH.md
@.planning/phases/02-boot-health-verification-and-live-state/02-02-SUMMARY.md
@RcloneMountManager.Core/Models/MountProfile.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed reliability preset catalog with managed-key scope</name>
  <files>RcloneMountManager.Core/Models/ReliabilityPolicyPreset.cs</files>
  <action>Create a typed immutable preset model and static catalog with stable IDs (`conservative`, `balanced`, `aggressive`) plus display metadata and key/value overrides. Define managed reliability keys explicitly as `vfs_cache_mode`, `dir_cache_time`, `attr_timeout`, `retries`, `low_level_retries`, and `retries_sleep`. Include helper lookup by id with fallback to `balanced`. Do not read/write raw `ExtraOptions` in this model; this catalog is the only source of truth for preset values.</action>
  <verify>dotnet build RcloneMountManager.slnx</verify>
  <done>Preset catalog is strongly typed, has stable IDs, and exposes managed reliability key scope required for non-clobber patching.</done>
</task>

<task type="auto">
  <name>Task 2: Extend profile model with selected reliability preset id</name>
  <files>RcloneMountManager.Core/Models/MountProfile.cs</files>
  <action>Add observable profile state `SelectedReliabilityPresetId` with default `balanced` so each profile carries explicit policy intent. Keep naming and `[ObservableProperty]` conventions aligned with existing model fields and avoid introducing side-channel state objects. This field must remain mount-profile scoped (not global) to satisfy POL-01.</action>
  <verify>dotnet build RcloneMountManager.slnx</verify>
  <done>`MountProfile` now includes per-profile selected preset id state ready for persistence and ViewModel binding.</done>
</task>

</tasks>

<verification>
Run `dotnet build RcloneMountManager.slnx` to confirm new model artifacts compile and are available to GUI/ViewModel layers.
</verification>

<success_criteria>
Typed reliability preset definitions and per-profile selected preset identity are in place as the foundation for apply/persist UI behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/04-per-profile-reliability-policy-presets/04-01-SUMMARY.md`
</output>
