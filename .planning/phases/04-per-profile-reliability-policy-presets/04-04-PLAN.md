---
phase: 04-per-profile-reliability-policy-presets
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
gap_closure: true
must_haves:
  truths:
    - "User can visually verify reliability preset controls are usable in Profile Settings."
    - "User can confirm preset application remains profile-scoped and does not cross-apply to other profiles."
    - "User can confirm preset selection and managed reliability options persist across app restart."
  artifacts:
    - path: ".planning/phases/04-per-profile-reliability-policy-presets/04-per-profile-reliability-policy-presets-VERIFICATION.md"
      provides: "Human verification scenarios for runtime UI and restart persistence."
    - path: "RcloneMountManager.GUI/Views/MainWindow.axaml"
      provides: "Profile Settings reliability preset picker and Apply button bindings."
    - path: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      provides: "Preset apply and persistence/reload logic used during manual verification."
  key_links:
    - from: "RcloneMountManager.GUI/Views/MainWindow.axaml"
      to: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      via: "SelectedReliabilityPresetId and ApplyReliabilityPresetCommand bindings"
      pattern: "SelectedReliabilityPresetId|ApplyReliabilityPresetCommand"
    - from: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      to: "profiles.json"
      via: "SaveProfiles and LoadProfiles selected preset mapping"
      pattern: "SelectedReliabilityPresetId"
---

<objective>
Close Phase 4 verification gaps by completing the remaining human runtime checks captured in the verifier report.

Purpose: Final phase acceptance requires real UI interaction and restart validation that static/code-only verification cannot prove.
Output: Human-verified acceptance of reliability preset UI flow and restart persistence behavior for Phase 4.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-per-profile-reliability-policy-presets/04-per-profile-reliability-policy-presets-VERIFICATION.md
@RcloneMountManager.GUI/Views/MainWindow.axaml
@RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs
@RcloneMountManager.Tests/ViewModels/MainWindowViewModelPolicyPresetTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconfirm automated baseline before manual UAT</name>
  <files>RcloneMountManager.Tests/ViewModels/MainWindowViewModelPolicyPresetTests.cs</files>
  <action>Run targeted regression coverage for policy presets before manual verification to ensure runtime checks begin from a known-green baseline. Use existing test suite only; do not change implementation files unless tests expose a real regression.</action>
  <verify>dotnet test --filter MainWindowViewModelPolicyPresetTests</verify>
  <done>Targeted policy preset tests pass, confirming apply/non-clobber/persist/reload behavior remains green before human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 4 reliability preset picker/apply flow and persistence wiring are already implemented and code-verified; this checkpoint validates runtime UX behavior and restart persistence.</what-built>
  <how-to-verify>
    1) Launch the app and open Profile Settings for at least two profiles that have different mount option baselines.
    2) For profile A, choose a non-default reliability preset, click Apply, and confirm only managed reliability keys change while unrelated options remain unchanged.
    3) Switch to profile B and confirm profile A's preset/option changes did not leak into profile B.
    4) Save profiles, fully close the app, relaunch, reopen profile A settings, and confirm the selected preset is still selected and effective managed options remain applied.
    5) Repeat a quick profile switch after relaunch to confirm profile-scoped behavior remains consistent.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or provide the exact failing step and observed behavior.</resume-signal>
</task>

</tasks>

<verification>
- `dotnet test --filter MainWindowViewModelPolicyPresetTests` passes before manual checks.
- Human verification confirms both verifier-required runtime checks from `04-per-profile-reliability-policy-presets-VERIFICATION.md`.
</verification>

<success_criteria>
- Reliability preset controls are confirmed usable in the real UI.
- Preset application is confirmed profile-scoped in runtime interaction.
- Preset selection and managed option effects are confirmed persistent across full app restart.
</success_criteria>

<output>
After completion, create `.planning/phases/04-per-profile-reliability-policy-presets/04-04-SUMMARY.md`
</output>
