---
phase: 03-startup-diagnostics-and-log-isolation
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs
  - RcloneMountManager.Tests/ViewModels/MainWindowViewModelDiagnosticsTests.cs
autonomous: true
must_haves:
  truths:
    - "User can filter diagnostics by profile and quickly isolate one profile's startup failure path."
    - "User can switch between full session events and startup-only timeline events for the selected diagnostics profile."
    - "Filtered timeline ordering and profile isolation stay deterministic as new events arrive."
  artifacts:
    - path: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      provides: "Profile-filter and startup-only timeline projection state"
      contains: "startup-only"
    - path: "RcloneMountManager.Tests/ViewModels/MainWindowViewModelDiagnosticsTests.cs"
      provides: "Regression tests for diagnostics filtering and timeline projection"
      contains: "filter"
  key_links:
    - from: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      to: "typed event store"
      via: "projection refresh when filter values or events change"
      pattern: "Where\(|Refresh.*Log"
    - from: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      to: "startup monitor + startup verify event categories"
      via: "startup-only predicate"
      pattern: "Category"
---

<objective>
Implement deterministic diagnostics filtering and startup timeline projection in the ViewModel so profile-specific failure analysis is fast and reliable.

Purpose: Fulfill OBS-03 by making log isolation first-class and model-driven rather than text scanning.
Output: Filter state/projection logic in `MainWindowViewModel` and deterministic diagnostics filter tests.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-startup-diagnostics-and-log-isolation/03-RESEARCH.md
@.planning/phases/03-startup-diagnostics-and-log-isolation/03-01-SUMMARY.md
@RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add diagnostics filter state for profile and startup timeline scope</name>
  <files>RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs</files>
  <action>Add explicit diagnostics filter properties independent from mutable `SelectedProfile` UI editing context (for example: `SelectedDiagnosticsProfileId`, `StartupTimelineOnly`, and a diagnostics profile selector collection derived from `Profiles`). Default to selected profile when possible, but keep filter state stable if selection changes during async operations. Preserve existing runtime-state behavior and command availability rules.</action>
  <verify>dotnet build RcloneMountManager.slnx</verify>
  <done>ViewModel exposes dedicated, bindable diagnostics filter state that can isolate one profile and toggle startup-only scope.</done>
</task>

<task type="auto">
  <name>Task 2: Project filtered typed events into timeline rows</name>
  <files>RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs</files>
  <action>Implement projection logic that builds the visible diagnostics timeline from typed events using deterministic predicates: profile-id match + optional startup-category constraint. Keep ordering chronological by event timestamp and refresh projection when new events arrive, profile filter changes, startup-only toggle changes, profiles are added/removed, or selected profile changes. Keep canonical store typed; perform string formatting only for visible timeline rows.</action>
  <verify>dotnet build RcloneMountManager.slnx</verify>
  <done>Visible diagnostics timeline reflects current filters accurately and updates immediately as events are appended.</done>
</task>

<task type="auto">
  <name>Task 3: Extend diagnostics tests for filter correctness and startup isolation</name>
  <files>RcloneMountManager.Tests/ViewModels/MainWindowViewModelDiagnosticsTests.cs, RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs</files>
  <action>Add tests that verify: (1) profile filter shows only matching profile events, (2) startup-only mode includes startup verification/monitor initialization events and excludes manual/runtime refresh categories, (3) event order is timeline-correct, and (4) changing filter inputs recomputes visible timeline deterministically. Keep tests deterministic with injected timestamps/event feeds where needed.</action>
  <verify>dotnet test RcloneMountManager.slnx --filter "FullyQualifiedName~MainWindowViewModelDiagnosticsTests"</verify>
  <done>OBS-03 filter behavior is covered by deterministic tests and protected from regressions.</done>
</task>

</tasks>

<verification>
Run `dotnet test RcloneMountManager.slnx --filter "FullyQualifiedName~MainWindowViewModelDiagnosticsTests"` and confirm profile-filter + startup-only timeline scenarios pass.
</verification>

<success_criteria>
The ViewModel can isolate one profile's startup failure path through explicit filter state and deterministic timeline projection.
</success_criteria>

<output>
After completion, create `.planning/phases/03-startup-diagnostics-and-log-isolation/03-02-SUMMARY.md`
</output>
