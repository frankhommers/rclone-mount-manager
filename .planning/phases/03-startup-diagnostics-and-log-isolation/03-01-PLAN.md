---
phase: 03-startup-diagnostics-and-log-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - RcloneMountManager.Core/Models/ProfileLogEvent.cs
  - RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs
  - RcloneMountManager.Tests/ViewModels/MainWindowViewModelDiagnosticsTests.cs
autonomous: true
must_haves:
  truths:
    - "Lifecycle and startup diagnostics are captured as typed events with real timestamps, not only formatted strings."
    - "Async operations always attribute log events to the originating profile even when selected profile changes mid-run."
    - "In-memory diagnostics remain bounded so long app sessions do not grow logs without limit."
  artifacts:
    - path: "RcloneMountManager.Core/Models/ProfileLogEvent.cs"
      provides: "Canonical typed profile diagnostics event model"
      contains: "record ProfileLogEvent"
    - path: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      provides: "Typed event ingestion pipeline with explicit profile routing"
      contains: "AppendLog(profileId"
    - path: "RcloneMountManager.Tests/ViewModels/MainWindowViewModelDiagnosticsTests.cs"
      provides: "Regression coverage for attribution and bounded storage behavior"
      contains: "selected profile changes"
  key_links:
    - from: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      to: "RcloneMountManager.Core/Models/ProfileLogEvent.cs"
      via: "typed event creation in append/log helpers"
      pattern: "new ProfileLogEvent"
    - from: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      to: "async mount/startup callbacks"
      via: "captured profile.Id passed into logging callback"
      pattern: "AppendLog\(profile\.Id"
---

<objective>
Establish a typed diagnostics event pipeline so startup/runtime lifecycle evidence is attributed to the correct profile and remains queryable.

Purpose: Create the OBS-02/OBS-03 foundation by replacing string-only activity storage with typed, profile-keyed lifecycle events.
Output: New `ProfileLogEvent` model, explicit-profile log routing in `MainWindowViewModel`, and deterministic diagnostics pipeline tests.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-startup-diagnostics-and-log-isolation/03-RESEARCH.md
@.planning/phases/02-boot-health-verification-and-live-state/02-03-SUMMARY.md
@RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical typed profile diagnostics event model</name>
  <files>RcloneMountManager.Core/Models/ProfileLogEvent.cs</files>
  <action>Create `ProfileLogEvent` as an immutable record with at least: `ProfileId`, `DateTimeOffset Timestamp`, `Category`, `Stage`, `Severity`, `Message`, and optional `Error`. Keep this model canonical for in-app diagnostics (not formatted display text). Use category/stage values that can represent startup verification, startup monitor initialization, runtime refresh, manual start, and manual stop.</action>
  <verify>dotnet build RcloneMountManager.slnx</verify>
  <done>Main codebase has a typed event artifact that can express timeline semantics required for startup diagnosis.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor ViewModel logging to typed per-profile event storage</name>
  <files>RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs</files>
  <action>Replace string-only `_profileLogs` internals with bounded typed storage keyed by `profile.Id` (`StringComparer.OrdinalIgnoreCase`, max 250 entries per profile). Keep existing user-visible activity list support by projecting typed events into display strings at the view-model edge. Update async call paths so every callback logs with explicit profile context (`AppendLog(profileId, ...)`) and remove dependence on `SelectedProfile` for async attribution. Ensure startup verification and startup monitor initialization emit explicit startup-category events for timeline isolation.</action>
  <verify>dotnet build RcloneMountManager.slnx</verify>
  <done>All lifecycle/startup logs are stored as typed events per profile with explicit attribution and bounded retention.</done>
</task>

<task type="auto">
  <name>Task 3: Add diagnostics pipeline tests for attribution and bounded retention</name>
  <files>RcloneMountManager.Tests/ViewModels/MainWindowViewModelDiagnosticsTests.cs, RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs</files>
  <action>Create focused tests proving: (1) async log callbacks remain attached to originating profile after `SelectedProfile` changes, (2) per-profile event storage enforces bounded cap without cross-profile bleed, and (3) startup verification emits startup-category events for each `StartAtLogin` profile. Use deterministic seams/delegates already used in runtime-state tests; avoid real timers or filesystem dependencies.</action>
  <verify>dotnet test RcloneMountManager.slnx --filter "FullyQualifiedName~MainWindowViewModelDiagnosticsTests"</verify>
  <done>Regression tests fail on profile misattribution or unbounded diagnostics event growth.</done>
</task>

</tasks>

<verification>
Run `dotnet test RcloneMountManager.slnx --filter "FullyQualifiedName~MainWindowViewModelDiagnosticsTests"` and confirm typed attribution + bounded-retention scenarios pass.
</verification>

<success_criteria>
The app now records startup/mount lifecycle evidence as typed, profile-attributed events that remain bounded and test-protected.
</success_criteria>

<output>
After completion, create `.planning/phases/03-startup-diagnostics-and-log-isolation/03-01-SUMMARY.md`
</output>
