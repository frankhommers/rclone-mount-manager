---
phase: 03-startup-diagnostics-and-log-isolation
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - RcloneMountManager.GUI/Views/MainWindow.axaml
  - RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs
  - RcloneMountManager.Tests/ViewModels/MainWindowViewModelDiagnosticsTests.cs
autonomous: true
must_haves:
  truths:
    - "User can view timestamped startup and lifecycle events directly in the main window."
    - "User can operate profile filter and startup-only controls from the diagnostics panel without leaving the screen."
    - "A profile's timeline reveals what failed and when through clear event timestamp, severity, stage, and message rows."
  artifacts:
    - path: "RcloneMountManager.GUI/Views/MainWindow.axaml"
      provides: "Diagnostics panel controls and timestamped timeline rendering"
      contains: "Activity"
    - path: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      provides: "UI-bound diagnostics row formatting and control bindings"
      contains: "Diagnostics"
    - path: "RcloneMountManager.Tests/ViewModels/MainWindowViewModelDiagnosticsTests.cs"
      provides: "Coverage that visible row content is timestamped and startup filters are honored"
      contains: "timestamp"
  key_links:
    - from: "RcloneMountManager.GUI/Views/MainWindow.axaml"
      to: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      via: "bindings for diagnostics filter controls and timeline items"
      pattern: "SelectedDiagnosticsProfileId|StartupTimelineOnly|Diagnostics"
    - from: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      to: "typed ProfileLogEvent store"
      via: "row text projection with timestamp/stage/severity/message"
      pattern: "Timestamp|Severity|Stage|Message"
---

<objective>
Deliver the in-app diagnostics panel UX so users can inspect timestamped lifecycle evidence and isolate startup failure timelines quickly.

Purpose: Complete OBS-02 and the user-visible portion of OBS-03 in the main window experience.
Output: Updated diagnostics UI in `MainWindow.axaml`, final timeline binding wiring, and tests validating visible diagnostics behavior.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-startup-diagnostics-and-log-isolation/03-RESEARCH.md
@.planning/phases/03-startup-diagnostics-and-log-isolation/03-02-SUMMARY.md
@RcloneMountManager.GUI/Views/MainWindow.axaml
@RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace activity list with diagnostics timeline panel controls</name>
  <files>RcloneMountManager.GUI/Views/MainWindow.axaml</files>
  <action>Refactor the existing Activity section to include diagnostics controls above the timeline: profile filter selector and startup-only toggle while preserving current layout style. Keep baseline `ListBox` rendering (no `DataGrid` package in this phase). Ensure control labels make startup-path isolation obvious and remain usable in existing window constraints.</action>
  <verify>dotnet build RcloneMountManager.slnx</verify>
  <done>Main window exposes profile + startup-scope controls directly in diagnostics UI.</done>
</task>

<task type="auto">
  <name>Task 2: Bind timestamped diagnostics rows to typed projection output</name>
  <files>RcloneMountManager.GUI/Views/MainWindow.axaml, RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs</files>
  <action>Bind diagnostics list to the filtered typed projection from plan 03-02 and display row content that includes timestamp, severity, stage/category, and message. Format timestamps from `DateTimeOffset` at projection/render edge (not canonical storage) and keep compatibility by preserving existing status/footer behavior. Ensure empty-state behavior is explicit (for example, "No diagnostics for current filter") rather than blank ambiguity.</action>
  <verify>dotnet build RcloneMountManager.slnx</verify>
  <done>Users can read when events happened and what failed from structured timeline rows without parsing raw ad-hoc text.</done>
</task>

<task type="auto">
  <name>Task 3: Add diagnostics UI-facing regression assertions</name>
  <files>RcloneMountManager.Tests/ViewModels/MainWindowViewModelDiagnosticsTests.cs, RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs</files>
  <action>Extend diagnostics tests to assert row output behavior expected by UI bindings: timestamp presence on projected rows, startup-only filtering reflected in visible rows, and profile switching does not leak events from other profiles. Keep tests at ViewModel level (no Avalonia UI harness) by asserting bound collections/properties consumed by XAML.</action>
  <verify>dotnet test RcloneMountManager.slnx --filter "FullyQualifiedName~MainWindowViewModelDiagnosticsTests"</verify>
  <done>In-app diagnostics presentation behavior remains stable and matches OBS-02/OBS-03 user expectations.</done>
</task>

</tasks>

<verification>
Run `dotnet test RcloneMountManager.slnx --filter "FullyQualifiedName~MainWindowViewModelDiagnosticsTests"` then `dotnet build RcloneMountManager.slnx` to validate final diagnostics UI binding integration.
</verification>

<success_criteria>
Main window now shows timestamped lifecycle diagnostics with profile/startup filters so users can follow one profile's startup failure timeline quickly.
</success_criteria>

<output>
After completion, create `.planning/phases/03-startup-diagnostics-and-log-isolation/03-03-SUMMARY.md`
</output>
