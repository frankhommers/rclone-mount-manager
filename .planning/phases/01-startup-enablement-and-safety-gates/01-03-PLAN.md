---
phase: 01-startup-enablement-and-safety-gates
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs
  - RcloneMountManager.GUI/Views/MainWindow.axaml
  - RcloneMountManager.Tests/ViewModels/MainWindowViewModelStartupTests.cs
autonomous: true
must_haves:
  truths:
    - "User cannot enable start at login when critical preflight checks fail."
    - "User can disable start at login without breaking manual start/stop mount actions."
    - "Startup toggle state persists immediately and remains after app restart/reboot."
    - "User can run preflight on demand and see explicit failure reasons in-app."
  artifacts:
    - path: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      provides: "Preflight-gated startup toggle orchestration and persistence wiring"
      contains: "ToggleStartupAsync"
    - path: "RcloneMountManager.GUI/Views/MainWindow.axaml"
      provides: "UI actions for preflight and startup toggling"
      contains: "Run startup preflight"
    - path: "RcloneMountManager.Tests/ViewModels/MainWindowViewModelStartupTests.cs"
      provides: "Behavior coverage for preflight gate and persistence semantics"
      contains: "SAFE-03"
  key_links:
    - from: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      to: "RcloneMountManager.Core/Services/StartupPreflightService.cs"
      via: "preflight before enable in ToggleStartupAsync and explicit preflight command"
      pattern: "RunAsync"
    - from: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      to: "RcloneMountManager.Core/Services/LaunchAgentService.cs"
      via: "enable/disable only after gate and before persistence"
      pattern: "EnableAsync|DisableAsync"
    - from: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      to: "profiles.json"
      via: "SaveProfiles called only after successful toggle operation"
      pattern: "SaveProfiles"
---

<objective>
Wire startup preflight and launch-agent behavior into the UI/viewmodel flow so startup toggling is gated, explicit, and safely persisted.

Purpose: Close BOOT-02, BOOT-03, and SAFE-03 by enforcing enablement gates and persistence ordering without touching manual mount control paths.
Output: Updated startup UI + ViewModel orchestration and startup-focused ViewModel tests.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-startup-enablement-and-safety-gates/01-RESEARCH.md
@.planning/phases/01-startup-enablement-and-safety-gates/01-01-SUMMARY.md
@.planning/phases/01-startup-enablement-and-safety-gates/01-02-SUMMARY.md
@RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs
@RcloneMountManager.GUI/Views/MainWindow.axaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit startup preflight command and user-visible report state</name>
  <files>RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs, RcloneMountManager.GUI/Views/MainWindow.axaml</files>
  <action>Add a `RunStartupPreflightCommand` in the ViewModel that runs `StartupPreflightService` for the selected profile, stores latest preflight report state, and writes explicit check outcomes to status/log output. Surface this action in `MainWindow.axaml` near startup controls with clear result text so SAFE-01 is user-invokable independently of toggle.</action>
  <verify>dotnet build RcloneMountManager.slnx</verify>
  <done>User can trigger preflight without enabling startup and can read explicit check results directly in the app.</done>
</task>

<task type="auto">
  <name>Task 2: Gate startup enablement and persist only after successful apply</name>
  <files>RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs</files>
  <action>Refactor `ToggleStartupAsync` into strict order: `preflight -> gate -> launch-agent apply -> persist`. On enable, block when `CriticalChecksPassed` is false and surface exact failed checks; do not call `EnableAsync` or mutate `StartAtLogin` in that case. On successful enable/disable, update `StartAtLogin`, call `SaveProfiles` immediately, and refresh startup button state. Keep `StartMountCommand`/`StopMountCommand` execution path unchanged to preserve manual mount behavior.</action>
  <verify>dotnet test RcloneMountManager.slnx --filter "ClassName=MainWindowViewModelStartupTests"</verify>
  <done>Enable is impossible with critical preflight failures; successful toggles persist to profiles and do not alter manual mount command behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add startup workflow ViewModel regression tests</name>
  <files>RcloneMountManager.Tests/ViewModels/MainWindowViewModelStartupTests.cs, RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs</files>
  <action>Add tests for: failed critical preflight blocks enable and keeps `StartAtLogin` false, successful enable persists startup flag, disable path clears and persists flag, and manual mount command guards remain unchanged after toggles. Introduce minimal test seams (constructor overloads or internal delegates) only where needed for deterministic tests; avoid broad DI rewrites.</action>
  <verify>dotnet test RcloneMountManager.slnx --filter "ClassName=MainWindowViewModelStartupTests"</verify>
  <done>Tests reliably detect regressions in SAFE-03 gating, BOOT-03 persistence ordering, and BOOT-02 manual workflow isolation.</done>
</task>

</tasks>

<verification>
Run `dotnet test RcloneMountManager.slnx` and confirm the new startup-focused suites pass alongside existing tests.
</verification>

<success_criteria>
Startup toggle flow is preflight-gated, state is persisted only after successful launch-agent operations, and users can run/read explicit preflight results in the UI.
</success_criteria>

<output>
After completion, create `.planning/phases/01-startup-enablement-and-safety-gates/01-03-SUMMARY.md`
</output>
