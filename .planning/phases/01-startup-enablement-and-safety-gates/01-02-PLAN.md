---
phase: 01-startup-enablement-and-safety-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - RcloneMountManager.Core/Services/LaunchAgentService.cs
  - RcloneMountManager.Tests/Services/LaunchAgentServiceTests.cs
autonomous: true
must_haves:
  truths:
    - "Enabling startup uses modern launchctl operations in the current user domain."
    - "LaunchAgent plist is validated before activation, and activation failures are explicit."
    - "Disabling startup only removes startup registration and does not alter manual mount behavior."
  artifacts:
    - path: "RcloneMountManager.Core/Services/LaunchAgentService.cs"
      provides: "LaunchAgent write/lint/bootstrap/bootout orchestration"
      exports:
        - "EnableAsync"
        - "DisableAsync"
        - "IsEnabled"
    - path: "RcloneMountManager.Tests/Services/LaunchAgentServiceTests.cs"
      provides: "Regression coverage for launchctl/plutil startup orchestration"
      contains: "bootstrap"
  key_links:
    - from: "RcloneMountManager.Core/Services/LaunchAgentService.cs"
      to: "launchctl gui/<uid> domain"
      via: "bootstrap/bootout commands"
      pattern: "bootstrap|bootout|gui/"
    - from: "RcloneMountManager.Core/Services/LaunchAgentService.cs"
      to: "plutil"
      via: "plist syntax validation before bootstrap"
      pattern: "plutil|lint"
---

<objective>
Modernize LaunchAgent activation/deactivation so startup registration is reliable, validated, and explicit on failure.

Purpose: Satisfy BOOT-01 and protect BOOT-02 by making startup registration robust while keeping manual mount lifecycle untouched.
Output: Updated LaunchAgent service path (`bootstrap/bootout` + plist lint) with regression tests.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-startup-enablement-and-safety-gates/01-RESEARCH.md
@RcloneMountManager.Core/Services/LaunchAgentService.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace legacy launchctl load/unload with domain-targeted bootstrap/bootout</name>
  <files>RcloneMountManager.Core/Services/LaunchAgentService.cs</files>
  <action>Update startup enable/disable operations to use modern launchctl commands: `bootstrap gui/&lt;uid&gt; &lt;plist&gt;` for enable and `bootout gui/&lt;uid&gt;/&lt;label&gt;` for disable. Build and reuse a single label format `com.rclonemountmanager.profile.&lt;id&gt;` and explicit target domain helper. Keep script generation and plist location behavior unchanged to avoid collateral workflow drift.</action>
  <verify>dotnet build RcloneMountManager.slnx</verify>
  <done>Service no longer calls `launchctl load/unload` as the primary path and uses explicit gui domain targeting.</done>
</task>

<task type="auto">
  <name>Task 2: Add plist lint and strict process result validation</name>
  <files>RcloneMountManager.Core/Services/LaunchAgentService.cs</files>
  <action>Run `plutil -lint` against generated plist before any bootstrap call, and enforce non-zero exit handling for both `plutil` and `launchctl` by throwing `InvalidOperationException` with stderr/stdout context. Keep profile mutation responsibilities outside this service; this class should only apply system changes and report exact failure causes.</action>
  <verify>dotnet test RcloneMountManager.slnx --filter "ClassName=LaunchAgentServiceTests"</verify>
  <done>Malformed plist or failed launchctl commands produce explicit exceptions, preventing silent startup misconfiguration.</done>
</task>

<task type="auto">
  <name>Task 3: Add LaunchAgent service regression tests</name>
  <files>RcloneMountManager.Tests/Services/LaunchAgentServiceTests.cs</files>
  <action>Create focused tests covering label/plist generation consistency, command argument construction for bootstrap/bootout target domains, and error propagation when lint/launch commands fail. Introduce minimal test seams in service only where required for deterministic testing (for example, injectable command runner delegate), and avoid broad architecture refactors.</action>
  <verify>dotnet test RcloneMountManager.slnx --filter "ClassName=LaunchAgentServiceTests"</verify>
  <done>Tests protect modern launchctl wiring, plist lint gating, and explicit failure reporting against regressions.</done>
</task>

</tasks>

<verification>
Run `dotnet test RcloneMountManager.slnx --filter "ClassName=LaunchAgentServiceTests"` and confirm launch-agent orchestration tests pass.
</verification>

<success_criteria>
Startup enable/disable path uses validated plist + modern launchctl calls with explicit failure surfaces and no dependency on legacy load/unload behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-startup-enablement-and-safety-gates/01-02-SUMMARY.md`
</output>
