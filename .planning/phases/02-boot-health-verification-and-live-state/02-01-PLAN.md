---
phase: 02-boot-health-verification-and-live-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - RcloneMountManager.Core/Models/MountLifecycleState.cs
  - RcloneMountManager.Core/Models/MountHealthState.cs
  - RcloneMountManager.Core/Models/ProfileRuntimeState.cs
  - RcloneMountManager.Core/Models/MountProfile.cs
  - RcloneMountManager.Core/Services/MountHealthService.cs
  - RcloneMountManager.Tests/Services/MountHealthServiceTests.cs
autonomous: true
must_haves:
  truths:
    - "Health checks classify each profile as healthy, degraded, or failed using mount usability truth rather than status text."
    - "Mounted but unusable profiles appear as degraded (not failed) so users can distinguish partial availability from complete outage."
    - "Health verification does not hang indefinitely when mount paths are slow or unresponsive."
  artifacts:
    - path: "RcloneMountManager.Core/Models/ProfileRuntimeState.cs"
      provides: "Typed runtime state snapshot for lifecycle + health + timestamp + error"
      contains: "record ProfileRuntimeState"
    - path: "RcloneMountManager.Core/Services/MountHealthService.cs"
      provides: "Bounded mount presence/usability verification"
      exports:
        - "VerifyAsync"
        - "VerifyAllAsync"
    - path: "RcloneMountManager.Tests/Services/MountHealthServiceTests.cs"
      provides: "Regression coverage for healthy/degraded/failed classification"
      contains: "mounted but unusable"
  key_links:
    - from: "RcloneMountManager.Core/Services/MountHealthService.cs"
      to: "RcloneMountManager.Core/Services/MountManagerService.cs"
      via: "presence and process-state probes"
      pattern: "IsMountedAsync|IsRunning"
    - from: "RcloneMountManager.Core/Services/MountHealthService.cs"
      to: "System.IO.Directory"
      via: "bounded usability probe"
      pattern: "EnumerateFileSystemEntries|WaitAsync"
---

<objective>
Create the runtime state domain and health verification service so post-login status is computed from explicit probes instead of stale booleans.

Purpose: Satisfy HEAL-01 and HEAL-02 foundations by making health truth deterministic, typed, and timeout-bounded.
Output: New runtime state models, `MountHealthService`, and service tests covering healthy/degraded/failed edge cases.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-boot-health-verification-and-live-state/02-RESEARCH.md
@RcloneMountManager.Core/Models/MountProfile.cs
@RcloneMountManager.Core/Services/MountManagerService.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed lifecycle and health runtime models</name>
  <files>RcloneMountManager.Core/Models/MountLifecycleState.cs, RcloneMountManager.Core/Models/MountHealthState.cs, RcloneMountManager.Core/Models/ProfileRuntimeState.cs, RcloneMountManager.Core/Models/MountProfile.cs</files>
  <action>Create explicit enums `MountLifecycleState` (`Idle`, `Mounting`, `Mounted`, `Failed`) and `MountHealthState` (`Unknown`, `Healthy`, `Degraded`, `Failed`) plus immutable `ProfileRuntimeState` containing lifecycle, health, `LastCheckedAt`, and optional error text. Extend `MountProfile` with a typed runtime state property (or equivalent strongly typed members) while keeping backward-compatible persisted profile fields untouched in this plan.</action>
  <verify>dotnet build RcloneMountManager.slnx</verify>
  <done>Core model layer can represent lifecycle and health independently, including mounted-but-degraded states.</done>
</task>

<task type="auto">
  <name>Task 2: Implement bounded mount health verification service</name>
  <files>RcloneMountManager.Core/Services/MountHealthService.cs</files>
  <action>Implement `MountHealthService` with `VerifyAsync(MountProfile, CancellationToken)` and `VerifyAllAsync(IEnumerable&lt;MountProfile&gt;, CancellationToken)`. Combine mount presence (`IsMountedAsync`), running-process signal (`IsRunning`), and a bounded mount-path usability probe (`Directory.EnumerateFileSystemEntries` wrapped in `Task.WaitAsync`). Classify outcomes as: not mounted => `Failed`; mounted + usable => `Healthy`; mounted + unusable/timeout => `Degraded`; surface probe exceptions as typed failure/degraded results instead of throwing unhandled exceptions.</action>
  <verify>dotnet build RcloneMountManager.slnx</verify>
  <done>Service returns deterministic runtime state results for single and multi-profile verification without hanging UI flow.</done>
</task>

<task type="auto">
  <name>Task 3: Add health classification regression tests</name>
  <files>RcloneMountManager.Tests/Services/MountHealthServiceTests.cs</files>
  <action>Add tests for at least: mounted+usable => healthy, mounted+unusable => degraded, mounted+probe-timeout => degraded, not-mounted => failed, and command/probe exceptions produce typed failed/degraded results with error text. Use deterministic seams (delegates/fakes) for presence and usability probes so tests do not depend on host machine mount state.</action>
  <verify>dotnet test RcloneMountManager.slnx --filter "FullyQualifiedName~MountHealthServiceTests"</verify>
  <done>Service tests fail on any regression in HEAL-01/HEAL-02 classification semantics.</done>
</task>

</tasks>

<verification>
Run `dotnet test RcloneMountManager.slnx --filter "FullyQualifiedName~MountHealthServiceTests"` and confirm healthy/degraded/failed classification scenarios pass.
</verification>

<success_criteria>
Mount health truth is available as a typed runtime result that distinguishes failed from degraded outcomes and enforces bounded probe time.
</success_criteria>

<output>
After completion, create `.planning/phases/02-boot-health-verification-and-live-state/02-01-SUMMARY.md`
</output>
