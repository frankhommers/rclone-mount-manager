---
phase: 02-boot-health-verification-and-live-state
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs
  - RcloneMountManager.GUI/App.axaml.cs
  - RcloneMountManager.Tests/ViewModels/MainWindowViewModelRuntimeStateTests.cs
autonomous: true
must_haves:
  truths:
    - "After app launch/login, every StartAtLogin profile receives a health verification pass without user clicking refresh."
    - "Users can see degraded or failed state when startup mounts do not pass usability checks."
    - "Live profile state keeps updating during normal operation instead of remaining stale after initial load."
  artifacts:
    - path: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      provides: "startup fan-out verification and periodic live-state refresh loop"
      contains: "PeriodicTimer"
    - path: "RcloneMountManager.GUI/App.axaml.cs"
      provides: "application startup trigger for runtime monitoring"
      contains: "MainWindowViewModel"
    - path: "RcloneMountManager.Tests/ViewModels/MainWindowViewModelRuntimeStateTests.cs"
      provides: "tests for startup fan-out and degraded/failed mapping"
      contains: "StartAtLogin"
  key_links:
    - from: "RcloneMountManager.GUI/App.axaml.cs"
      to: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      via: "startup invocation of runtime monitoring"
      pattern: "Initialize|Start.*Runtime"
    - from: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      to: "RcloneMountManager.Core/Services/MountHealthService.cs"
      via: "fan-out verify for StartAtLogin profiles and periodic checks"
      pattern: "VerifyAllAsync"
---

<objective>
Add boot-time fan-out verification and a periodic live-state update loop so runtime truth remains current after login and during ongoing app use.

Purpose: Complete HEAL-01/HEAL-02 post-login behavior and keep OBS-01 state fresh without manual refresh.
Output: Startup-triggered runtime verification, periodic health refresh loop, and regression tests for startup-enabled profiles.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-boot-health-verification-and-live-state/02-RESEARCH.md
@.planning/phases/01-startup-enablement-and-safety-gates/01-03-SUMMARY.md
@.planning/phases/02-boot-health-verification-and-live-state/02-02-SUMMARY.md
@RcloneMountManager.GUI/App.axaml.cs
@RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add startup fan-out verification for StartAtLogin profiles</name>
  <files>RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs, RcloneMountManager.GUI/App.axaml.cs</files>
  <action>Add an explicit runtime-monitor startup entry point in `MainWindowViewModel` and invoke it from app initialization once the main window DataContext is created. On startup, verify all profiles with `StartAtLogin = true`, apply health/lifecycle results per profile, and log startup verification outcomes. Do not parse `launchctl print` output for logic; rely only on service probes and existing startup flags.</action>
  <verify>dotnet build RcloneMountManager.slnx</verify>
  <done>Startup-enabled profiles receive automatic post-login verification and state publication without user action.</done>
</task>

<task type="auto">
  <name>Task 2: Implement periodic live-state refresh loop with cancellation</name>
  <files>RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs</files>
  <action>Implement background periodic verification (2-5s cadence) using `PeriodicTimer` and cooperative cancellation so all profile runtime states are refreshed continuously. Marshal UI updates safely to Avalonia UI thread when applying snapshot changes, and ensure loop cleanup occurs on app shutdown/disposal path to avoid orphaned tasks.</action>
  <verify>dotnet test RcloneMountManager.slnx --filter "FullyQualifiedName~MainWindowViewModelRuntimeStateTests"</verify>
  <done>Runtime state timestamps and health/lifecycle values keep advancing while app is open, with no UI-thread blocking.</done>
</task>

<task type="auto">
  <name>Task 3: Extend ViewModel runtime tests for startup fan-out and live updates</name>
  <files>RcloneMountManager.Tests/ViewModels/MainWindowViewModelRuntimeStateTests.cs, RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs</files>
  <action>Extend runtime-state tests to cover: startup fan-out verifies only `StartAtLogin` profiles, failed/degraded startup checks map to visible runtime states, and periodic refresh applies updated state snapshots across multiple profiles. Use deterministic fake health snapshots and controllable tick seams; avoid timing-flaky real-time waits.</action>
  <verify>dotnet test RcloneMountManager.slnx --filter "FullyQualifiedName~MainWindowViewModelRuntimeStateTests"</verify>
  <done>Post-login health verification and ongoing live update behavior are regression-protected for HEAL and OBS requirements.</done>
</task>

</tasks>

<verification>
Run `dotnet test RcloneMountManager.slnx --filter "FullyQualifiedName~MainWindowViewModelRuntimeStateTests"` then run `dotnet build RcloneMountManager.slnx` to confirm startup fan-out and periodic loop integration compile and pass targeted tests.
</verification>

<success_criteria>
After app launch, startup-enabled profiles are automatically verified, degraded/failed outcomes are visible, and runtime state stays live without manual refresh.
</success_criteria>

<output>
After completion, create `.planning/phases/02-boot-health-verification-and-live-state/02-03-SUMMARY.md`
</output>
