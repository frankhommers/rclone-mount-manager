---
phase: 02-boot-health-verification-and-live-state
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs
  - RcloneMountManager.GUI/Views/MainWindow.axaml
  - RcloneMountManager.Tests/ViewModels/MainWindowViewModelRuntimeStateTests.cs
autonomous: true
must_haves:
  truths:
    - "Users can see per-profile lifecycle transitions (idle, mounting, mounted, failed) during mount and unmount actions."
    - "Users can see each profile's current health verdict (healthy, degraded, failed) in the UI."
    - "Runtime state transitions are driven by typed state updates rather than free-form status strings."
  artifacts:
    - path: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      provides: "Lifecycle transition orchestration and runtime-state-to-UI mapping"
      contains: "StartMountAsync"
    - path: "RcloneMountManager.GUI/Views/MainWindow.axaml"
      provides: "Visible lifecycle and health state presentation"
      contains: "Runtime state"
    - path: "RcloneMountManager.Tests/ViewModels/MainWindowViewModelRuntimeStateTests.cs"
      provides: "Regression coverage for lifecycle transitions and failure mapping"
      contains: "idle, mounting, mounted, failed"
  key_links:
    - from: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      to: "RcloneMountManager.Core/Services/MountHealthService.cs"
      via: "refresh/state recomputation after mount actions"
      pattern: "VerifyAsync|VerifyAllAsync"
    - from: "RcloneMountManager.GUI/Views/MainWindow.axaml"
      to: "RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs"
      via: "bindings for lifecycle and health text/badges"
      pattern: "Runtime|Health|Lifecycle"
---

<objective>
Wire runtime lifecycle and health state into ViewModel action flow and UI bindings so users can see truthful per-profile state transitions.

Purpose: Satisfy OBS-01 and expose HEAL outcomes in the primary interface during normal operation.
Output: Updated ViewModel transition logic, runtime-state UI elements, and targeted ViewModel tests.
</objective>

<execution_context>
./.opencode/get-shit-done/workflows/execute-plan.md
./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-boot-health-verification-and-live-state/02-RESEARCH.md
@.planning/phases/02-boot-health-verification-and-live-state/02-01-SUMMARY.md
@RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs
@RcloneMountManager.GUI/Views/MainWindow.axaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lifecycle transition hooks around mount commands</name>
  <files>RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs</files>
  <action>Refactor mount command orchestration so selected profile lifecycle transitions are explicit: set `Mounting` before `StartAsync`, set `Mounted`/`Failed` based on post-action verification, set `Idle` after successful stop, and preserve failure state when command exceptions occur. Keep existing `RunBusyActionAsync` and log patterns, but replace `LastStatus = "Mounted: ..., Running: ..."` as primary truth with typed runtime state updates and mapped user-facing text.</action>
  <verify>dotnet build RcloneMountManager.slnx</verify>
  <done>ViewModel transitions lifecycle state deterministically for start/stop/refresh flows without relying on ad-hoc status strings.</done>
</task>

<task type="auto">
  <name>Task 2: Surface lifecycle and health state in main UI</name>
  <files>RcloneMountManager.GUI/Views/MainWindow.axaml, RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs</files>
  <action>Add visible runtime-state presentation in the main window: show lifecycle + health for the selected profile and include concise state signal in profile list rows. Bind to typed ViewModel properties (for example, display text helpers or state enums converted to text) and ensure startup preflight panel remains intact. Keep layout changes localized to existing profile/actions sections.</action>
  <verify>dotnet build RcloneMountManager.slnx</verify>
  <done>User can read current lifecycle and health for profiles directly in the UI during normal operations.</done>
</task>

<task type="auto">
  <name>Task 3: Add runtime state transition ViewModel tests</name>
  <files>RcloneMountManager.Tests/ViewModels/MainWindowViewModelRuntimeStateTests.cs, RcloneMountManager.GUI/ViewModels/MainWindowViewModel.cs</files>
  <action>Create tests covering state transitions for success and failure paths: start sets mounting then mounted/failed, stop reaches idle when unmounted, refresh maps degraded/failed health correctly, and status text reflects runtime state output. Add only minimal constructor seams required to inject deterministic health verification results.</action>
  <verify>dotnet test RcloneMountManager.slnx --filter "FullyQualifiedName~MainWindowViewModelRuntimeStateTests"</verify>
  <done>OBS-01 transition behavior is regression-protected by deterministic ViewModel tests.</done>
</task>

</tasks>

<verification>
Run `dotnet test RcloneMountManager.slnx --filter "FullyQualifiedName~MainWindowViewModelRuntimeStateTests"` and validate lifecycle/health transition assertions pass.
</verification>

<success_criteria>
Main window reflects real per-profile lifecycle and health state transitions in both actions flow and read-only status presentation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-boot-health-verification-and-live-state/02-02-SUMMARY.md`
</output>
